COMO EJECUTAR EL PROGRAMA:

Para ejecutar el programa desde la consola, ir hasta la carpeta dist del 
proyecto y ejecutar el siguiente comando:

java -jar "GasolineraProject.jar"

Una vez ejecutado, seguir las instrucciones que saldrán por consola.

Notas:

*Si la ultima variable del Simulated Annealing se trata de un decimal,
indicar la parte decimal con una coma "," y no con un punto.